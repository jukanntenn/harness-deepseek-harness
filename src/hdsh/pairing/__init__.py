"""Bilingual documentation pairing.

The submodules separate concerns by concept: ``manifest`` holds the pairing
manifest format, ``corpus`` the scope predicates, ``records`` the
consistency-record byte format, ``links`` locale-aware link resolution,
``structure`` the generated-region and structural-signature grammar, ``git``
repository-owned Git operations, ``verify`` the gate, and ``merge`` the
fail-closed merge driver.
"""
