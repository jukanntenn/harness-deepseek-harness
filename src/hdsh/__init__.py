"""Reusable harness-framework governance gates.

The package ships the executable half of three mechanisms: GitHub issue/PR
policy (``hdsh.policy``), bilingual documentation pairing (``hdsh.pairing``),
and documentation and pull-request workflow tooling (``hdsh.docs``,
``hdsh.rfc``, ``hdsh.scope``, ``hdsh.worktree``). Every command runs through
the unified ``hdsh`` entry point; the prose contracts live in the repository
documentation — see ``docs/i18n/README.md`` and ``.agents/rfcs/``.
"""

__version__ = "0.1.0"
