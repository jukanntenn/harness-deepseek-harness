# AGENTS.md

TODO(adopt): This file carries the standing orders an agent needs in every session for this repository. Keep it lean: link the owning document instead of restating it.

## Commands

TODO(adopt): List the build, test, lint, and gate commands an agent runs for this repository, one line each.

<a id="run-relevant-checks-locally"></a>

TODO(adopt): State the rule for selecting the narrowest checks that cover the changed surface; exhaustive rehearsal belongs to CI.

## Conventions

TODO(adopt): State the conventions every change must follow, linking the owning documents: the [documentation standard](docs/AGENTS.md), the [bilingual documentation contract](docs/i18n/README.md), and the [RFC rules](.agents/rfcs/README.md).

## Workflow

TODO(adopt): Point at the workflow skills under `.agents/skills/` that this repository relies on, and state the issue-first and RFC-in-the-same-PR rules in one line each.
