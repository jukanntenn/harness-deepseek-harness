# Development guide

English | [中文](development.zh.md)

The setup tutorial takes a new contributor from prerequisites to a checked checkout. The contributor reference that follows covers the daily workflow and the repository's tool integrations.

## Setup tutorial

### Prerequisites

TODO(adopt): List the runtime and toolchain prerequisites — language versions, package manager, build tools — with download links.

### First-time setup

TODO(adopt): Give the exact commands that create the environment from a fresh clone, and state the observable condition that marks setup complete.

## Contributor reference

### Project layout

One name identifies one concept across the directory, the interface, and any id prefix; the full component map is [architecture.md](architecture.md).

### Daily commands

TODO(adopt): List the commands a contributor runs most often, one line each on what it checks. The documentation gates and the bilingual pairing workflow follow the [bilingual documentation contract](i18n/README.md); the gate corpora are configured in `.hdsh/docs.manifest.json` and `.hdsh/pairing.manifest.json`.

### TODO markers

TODO(adopt): State the repository's TODO-marker convention by urgency, or remove this section if none applies.
